# DAY 6: Opening and Reading a File:
Write a short Python program that opens a file named "example.txt" and prints its content. 


*NOTE: only use built-in libraries of python (do not use pandas etc.)*
